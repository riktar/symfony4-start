nelmio_api_doc:
    documentation:
        info:
            title: La mia API
            description: Esempio di come scrivere una REST API con Symfony4
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
